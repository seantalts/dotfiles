[user]
	name = Sean Talts
	email = sean.talts@gmail.com
[color]
    diff = auto
	status = always
    branch = auto

[push]
    default = matching

[core]
	editor = /Users/xitrium/bin/vim

[merge]
    tool = mvim

[mergetool "mvim"]
    cmd=mvim -d -g $LOCAL $MERGED $REMOTE
    keepbackup=false
[alias]
	lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --
    st = status
    ci = commit
    co = checkout
[url "git://github.com/ghc/packages-"]
	insteadOf = git://github.com/ghc/packages/
